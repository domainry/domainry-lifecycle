// Package sqlite is the SQLite persistence profile boundary. Embedded
// Lifecycle deployments use the renderer supplied by the host and therefore
// require no source-owned driver or connection factory here.
package sqlite
